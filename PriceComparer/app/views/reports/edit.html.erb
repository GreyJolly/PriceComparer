<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Report</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
</head>
<body>

    <%= render "partials/header" %>

    <div class="container">
        <h1 class="center-align"><%= action_name.capitalize %> Report</h1>

        <%= form_with model: @report, local: true, html: { class: 'col s12' } do |form| %>
            <% if @report.errors.any? %>
                <div id="error_explanation" class="card-panel red lighten-4">
                    <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>
                    <ul>
                        <% @report.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="row">
                <div class="input-field col s12">
                    <%= form.text_field :title, class: "validate"%>
                    <%= form.label :title, "Title" %>
                </div>
            </div>

            <div class="row">
                <div class="input-field col s12">
                    <%= form.text_area :content, class: "materialize-textarea"%>
                    <%= form.label :content, "Content" %>
                </div>
            </div>

            <div class="row">
                <div class="col s12">
                    <%= form.submit "Salva", class: "btn waves-effect waves-light" %>
                    <%= link_to 'Indietro', reports_path, class: "btn-flat waves-effect" %>
                </div>
            </div>
        <% end %>
    </div>

</body>
</html>
