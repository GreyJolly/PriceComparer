<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Report</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_link_tag 'reports_edit', media: 'all', data: {turbolinks: false} %>
    <%= javascript_include_tag 'application' %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

</head>
<body>
    <%= render "partials/header" %>
    <div class="container-report">
        <h1 class="center-align h1-reports">Modifica segnalazione</h1>
        <%= form_with model: @report, local: true, html: { class: 'col s12' } do |form| %>
            <% if @report.errors.any? %>
                <div id="error_explanation" class="card-panel red lighten-4">
                    <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>
                    <ul>
                        <% @report.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="row">
                <div class="input-field col s12">
                    <%= form.text_field :title, class: "validate"%>
                    <%= form.label :title, "Titolo" %>
                </div>
            </div>

            <div class="row">
                <div class="input-field col s12">
                    <%= form.text_area :content, class: "materialize-textarea"%>
                    <%= form.label :content, "Descrizione" %>
                </div>
            </div>

            <div class="row">
                <div class="col s12 btn-group">
                    <%= form.submit "Salva", class: "btn waves-effect waves-light btn-edit" %>
                    <%= link_to 'Indietro', reports_path, class: "btn waves-effect waves-light btn-back" %>
                </div>
            </div>
        <% end %>
    </div>
</body>
</html>
